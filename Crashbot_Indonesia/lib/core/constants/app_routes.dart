/// Named route constants (§10 Navigasi).
/// Semua route didefinisikan di sini, bukan tersebar di Navigator.push().
class AppRoutes {
  AppRoutes._();

  static const String login = '/login';
  static const String lobby = '/lobby';
  static const String remote = '/remote';
}
